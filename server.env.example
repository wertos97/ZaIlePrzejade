# ============================================================
# Konfiguracja serwera MPK (szablon)
# ============================================================
# Skopiuj ten plik do `server.env` i dostosuj wartości:
#
#   cp server.env.example server.env
#
# UWAGA: `server.env` jest ignorowany przez git (nie trafi do repo),
# dzięki czemu nie udostępniasz szczegółów swojego serwera publicznie.
#
# Wszystkie zmienne są OPCJONALNE - jeśli nie ustawisz wartości,
# skrypty użyją bezpiecznych domyślnych wartości.

# Port, na którym działa aplikacja (domyślnie: 8080)
# Uwaga: server.py czyta PORT ze zmiennej środowiskowej.
PORT=8080

# Adres URL do sprawdzania zdrowia serwera (domyślnie: http://127.0.0.1:$PORT/api/health)
# Zwykle nie musisz tego zmieniać.
# HEALTH_URL=http://127.0.0.1:8080/api/health

# Nazwa gałęzi git do śledzenia aktualizacji (domyślnie: bieżąca gałąź)
# GIT_BRANCH=main

# Czy uruchamiać serwer w tle (nohup) - true/false (domyślnie: true)
# RUN_IN_BACKGROUND=true

# Czy ufać nagłówkom X-Real-IP / X-Forwarded-For (domyślnie: false).
# Włącz true TYLKO za reverse proxy, które nadpisuje te nagłówki - inaczej
# klient mógłby podszyć się pod inny adres IP i omijać limity zapytań.
# TRUST_PROXY_HEADERS=false

# Kanoniczny publiczny adres aplikacji (używany w meta OG i OG images).
# PUBLIC_BASE_URL=https://zaileprzeja.de

# Limity rozmiaru cache tras w bajtach (opcjonalne strojenie pod dostępne
# zasoby; domyślnie 20 MB dla wyszukiwań i 24 MB dla cache tras).
# FIND_CACHE_MAX_BYTES=20971520
# ROUTE_CACHE_MAX_BYTES=25165824

# Poziom logowania (DEBUG/INFO/WARNING/ERROR) i opcjonalny plik logów.
# LOG_LEVEL=INFO
# LOG_FILE=/var/log/mpk/app.log